node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local shard-db data + per-developer db.env never get committed.
# All paths inside any committed example db.env should be RELATIVE
# so the same config works on any machine. See scripts/dev-up.sh.
db/
db.env
db.env.*
!db.env.example
*.log
tmp/
.hn-refresh-state.json
.history